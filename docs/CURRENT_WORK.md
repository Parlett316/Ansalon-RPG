# Current work

Nothing in flight.

Milestone 65 (Staff of Striking/Curing) just shipped, closing out the
DLA-magic-items backlog item Milestone 64 pointed at -- see
`docs/MILESTONES.md`'s Milestone 65 entry for the full sourcing, the two
`AskUserQuestion` decisions (1d8 cure dice, Cleric-only quest reward), and
the charge-pool-cut design finding. Verified via a throwaway self-test, a
clean `/W4` rebuild, and a direct piped run confirming the user's real
save loads cleanly under the new save format. Interactive verification
(talking to the new Ruin-Scavenger NPC at Xak Tsaroth as a Cleric,
clearing the skeletons, equipping the staff, using its combat cure
action) still needs the user's own keyboard.
